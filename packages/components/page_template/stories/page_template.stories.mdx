import { Canvas, Story } from "@storybook/addon-docs";
import i18n from "i18next";
import { initReactI18next } from "react-i18next";

import { LoadingIndicator } from "@buoysoftware/anchor-loading-indicator";
import { DropdownMenu, DropdownMenuList, DropdownMenuItem } from "@buoysoftware/anchor-dropdown-menu";
import { Box, Flex } from "@buoysoftware/anchor-layout";
import { Breadcrumbs, Breadcrumb } from "@buoysoftware/anchor-breadcrumbs";
import { Button } from "@buoysoftware/anchor-button";

import { PageActionBar, PageBreadcrumbs, PageContent, PageFooter, PageHeader, PageTemplate } from "../src";

<Meta
  title="Templates / PageTemplate"
  component={PageTemplate}
  subcomponents={PageTemplate}
  decorators={[
    (Story) => {
      i18n.use(initReactI18next).init({
        lng: "en",
        resources: {
          en: {
            translation: {
              loading: "Loading"
            }
          }
        }
      });
      return <Story />
    }
  ]}
  parameters={{
    layout: "fullscreen"
  }}
/>

## Basic Usage

<Canvas>
  <Story name="Basic" args={{}}>
    <PageTemplate>
      <PageBreadcrumbs>
        <Breadcrumbs>
          <Breadcrumb>Questionnaires</Breadcrumb>
        </Breadcrumbs>
      </PageBreadcrumbs>
      <PageHeader title="Template example" />
      <PageContent>
        Page Content
      </PageContent>
    </PageTemplate>
  </Story>
</Canvas>

## With ActionBar

<Canvas>
  <Story name="with ActionBar" args={{}}>
    <PageTemplate>
      <PageBreadcrumbs>
        <Breadcrumbs>
          <Breadcrumb>Questionnaires</Breadcrumb>
        </Breadcrumbs>
      </PageBreadcrumbs>
      <PageActionBar justifyContent="flex-end">
        <DropdownMenu label="Actions">
          <DropdownMenuList>
            <DropdownMenuItem>Item 1</DropdownMenuItem>
            <DropdownMenuItem>Item 2</DropdownMenuItem>
            <DropdownMenuItem>Item 3</DropdownMenuItem>
            <DropdownMenuItem>Item 4</DropdownMenuItem>
          </DropdownMenuList>
        </DropdownMenu>
      </PageActionBar>
      <PageHeader title="Template example" />
      <PageContent>
        Page Content
      </PageContent>
    </PageTemplate>
  </Story>
</Canvas>

## With Footer

<Canvas>
  <Story name="with Footer" args={{}}>
    <PageTemplate>
      <PageBreadcrumbs>
        <Breadcrumbs>
          <Breadcrumb>Questionnaires</Breadcrumb>
        </Breadcrumbs>
      </PageBreadcrumbs>
      <PageHeader title="Template example" />
      <PageContent>
        Page Content
      </PageContent>
      <PageFooter>
        <Button>Cancel</Button>
        <Button colorScheme="primary">Save</Button>
      </PageFooter>
    </PageTemplate>
  </Story>
</Canvas>

## With Loader

<Canvas>
  <Story name="with Loader" args={{}}>
    <PageTemplate>
      <PageBreadcrumbs>
        <Breadcrumbs>
          <Breadcrumb>Questionnaires</Breadcrumb>
        </Breadcrumbs>
      </PageBreadcrumbs>
      <PageHeader title="Template example" />
      <PageContent loading>
        Content is loading
      </PageContent>
    </PageTemplate>
  </Story>
</Canvas>
