import { Meta, Canvas, Story } from "@storybook/addon-docs";

import { Breadcrumbs, Breadcrumb } from "../src";

<Meta
  title="Components / Breadcrumbs"
  component={Breadcrumbs}
  parameters={{
    layout: "centered",
  }}
/>

export const Template = (args) => {
  return(
    <Breadcrumbs>
      <Breadcrumb>Donors</Breadcrumb>
      <Breadcrumb>Donor 123</Breadcrumb>
      <Breadcrumb>Edit</Breadcrumb>
      <Breadcrumb></Breadcrumb>
    </Breadcrumbs>
  );
}

# Breadcrumbs

## Import

```javascript
import { Breadcrumbs, Breadcrumb } from "@buoysoftware/anchor-ui";
```

## Basic Usage

<Canvas>
  <Story args={{}} name="Breadcrumbs">
    {Template.bind({})}
  </Story>
</Canvas>
