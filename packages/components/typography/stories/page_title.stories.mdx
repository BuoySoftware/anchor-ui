import { Canvas, Story } from "@storybook/addon-docs";

import { PageTitle } from "../src"

<Meta
  title="Typography / Components / PageTitle"
  component={PageTitle}
  parameters={{
  }}
/>

export const Template = (args) => {
  return(
    <PageTitle {...args} />
  );
}

# PageTitle

The page title component composes the heading component with some spacing for
reuse as a page title.

## Import

```javascript
import { PageTitle } from "@buoysoftware/anchor-ui";
```

## Basic Usage

<Canvas>
  <Story
    name="PageTitle"
    args={{
      children: "Page Title"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
