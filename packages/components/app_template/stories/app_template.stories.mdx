import { Canvas, Story } from "@storybook/addon-docs";
import { Flex } from "@buoysoftware/anchor-layout";

import { AppTemplate, AppSideNav, AppContent } from "../src";

<Meta
  title="Templates / AppTemplate"
  component={AppTemplate}
  parameters={{
    layout: "fullscreen"
  }}
/>

export const Template = (args) => {
  return (
    <AppTemplate>
      <AppSideNav>
        <Flex color="white" bg="red" height="100vh" width="240px" alignItems="center" justifyContent="center">
          side-nav
        </Flex>
      </AppSideNav>
      <AppContent>
        <Flex color="white" bg="blue" height="100vh" alignItems="center" justifyContent="center">
          app-content
        </Flex>
      </AppContent>
    </AppTemplate>
  );
};

## Basic Usage

<Canvas>
  <Story name="AppTemplate" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
