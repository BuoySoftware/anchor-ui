{
  "name": "@buoysoftware/anchor-side-nav",
  "version": "0.33.4",
  "main": "src/index.ts",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@buoysoftware/anchor-layout": "^0.33.4",
    "polished": "^4.2.2"
  },
  "scripts": {
    "build": "tsc -outDir dist && yarn copy-files",
    "copy-files": "cp ./src/logo.png ./dist",
    "compile": "tsc --noEmit",
    "postpack": "clean-package restore",
    "prepack": "clean-package"
  }
}
