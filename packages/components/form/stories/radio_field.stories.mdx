import { Meta, Canvas, Story } from "@storybook/addon-docs";
import { useForm } from "react-hook-form";

import { Form, Label, RadioField } from "../src";

<Meta
  title="Components / Forms / RadioField"
  component={RadioField}
  parameters={{
    layout: "centered",
    controls: {
      include: ["disabled"]
    }
  }}
/>

export const Template = (args) => {
  const form = useForm();
  const options = [
    { value: "1", label: "Option 1" },
    { value: "2", label: "Option 2" },
    { value: "3", label: "Option 3" },
  ];
  return(
    <Form form={form} id="radio-field-example" scope="radio-field-example">
      <RadioField
        label={<Label>Radio Field</Label>}
        options={options}
        name="radioFieldExample"
        {...args} 
      />
    </Form>
  );
}

# RadioField

The radio field is intended for use within a `Form` component utilizing react-hook-form.
It is composed of the FormField and Radio components.

## Import

```javascript
import { RadioField } from "@buoysoftware/anchor-ui";
```

## Basic Usage

<Canvas>
  <Story args={{ disabled: false }} name="RadioField">
    {Template.bind({})}
  </Story>
</Canvas>
