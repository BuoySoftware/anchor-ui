import { Canvas, Story } from "@storybook/addon-docs";

import { ErrorMessage } from "../src";

<Meta
  title="Components / Forms / ErrorMessage"
  component={ErrorMessage}
  parameters={{
    layout: "centered",
    controls: {
      include: ["errors"]
    }
  }}
/>

export const Template = (args) => {
  return(
    <ErrorMessage {...args}/>
  );
}

## Basic Usage

<Canvas>
  <Story name="ErrorMessage" args={{ errors: ["A selection is required"]}}>
    {Template.bind({})}
  </Story>
</Canvas>
