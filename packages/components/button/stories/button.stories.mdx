import { Canvas, Story } from "@storybook/addon-docs";

import { Button } from "../src";

<Meta
  title="Components / Button"
  component={Button}
  parameters={{ layout: "centered" }}
/>

export const Template = (args) => {
  return(
    <Button {...args} />
  );
}

# Button

## Import

```javascript
import { Button } from "@buoysoftware/anchor-ui";
```

## Usage

<Canvas>
  <Story
    name="Basic"
    args={{ disabled: false, size: "l", children: "Button", colorScheme: "basic" }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
