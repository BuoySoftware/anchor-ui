import { ChevronDown, ChevronLeft } from "@styled-icons/feather";
import { Canvas, Story } from "@storybook/addon-docs";

import { Button } from "../src";

<Meta
  title="Components / Button"
  component={Button}
  parameters={{
    controls: {
      exclude: /icon/g
    },
    layout: "centered"
  }}
/>

export const Template = (args) => {
  return(
    <Button {...args} />
  );
}

# Button

## Import

```javascript
import { Button } from "@buoysoftware/anchor-ui";
```

## Basic Usage

<Canvas>
  <Story
    name="Basic"
    args={{
      disabled: false,
      size: "l",
      children: "Button",
      colorScheme: "basic" 
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Button with Icon Right

<Canvas>
  <Story
    name="Icon Right"
    args={{
      icon: <ChevronDown size="20px" strokeWidth="2px" />,
      iconPosition: "right",
      disabled: false,
      size: "l",
      children: "Button",
      colorScheme: "basic" 
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Button with Icon Left

<Canvas>
  <Story
    name="Icon Left"
    args={{
      icon: <ChevronLeft size="20px" strokeWidth="2px" />,
      iconPosition: "left",
      disabled: false,
      size: "l",
      children: "Button",
      colorScheme: "basic"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
