import { Canvas, Story } from "@storybook/addon-docs";
import { ActionBar } from "../src";

<Meta
  title="Components / Action Bar"
  component={ActionBar}
/>

export const Template = (args) => (
  <ActionBar {...args}>Actions In here</ActionBar>
);

# Action Bar

## Import

```javascript
import { ActionBar } from "@buoysoftware/anchor-ui";
```

## Usage

<Canvas>
  <Story name="Action Bar">{Template.bind({})}</Story>
</Canvas>
